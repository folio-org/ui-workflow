import React from 'react';
import { AppIcon } from '@folio/stripes/core';

/**
 * An icon used to represent the ui-workflow module.
 */
export const WorkflowIcon = () => {
  return <AppIcon app="@folio/user" iconKey="app" size="large" />;
};
