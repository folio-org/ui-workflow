/**
 * Provide properties for a detail pane.
 *
 * onClose: Event handler.
 * setShowDetail: Show detail setter.
 * showDetail: Value.
 */
export interface IDetailPaneProperties {
  onClose?: any;
  setShowDetail?: any;
  showDetail?: boolean;
}
