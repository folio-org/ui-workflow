/**
 * Provide properties for managing search state.
 *
 * view: The active view being used from the VIEW constant.
 * stripes: The stripes use data.
 */
export interface IDetailPane {
  view?: any;
  stripes?: any;
}
