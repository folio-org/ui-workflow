/**
 * Provide properties for the Workflow general pane action menu.
 *
 * @param deleteModal The popup modal for the delete action.
 * @param stripes The stripes use data.
 * @param view The active view being used from the VIEW constant.
 */
export interface IWorkflowGeneralActionMenu {
  deleteModal?: any;
  stripes?: any;
  view?: any;
}
